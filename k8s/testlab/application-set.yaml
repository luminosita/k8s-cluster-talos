apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: testlab-ejabberd
  namespace: argocd
  labels:
    me.kundun: testlab-ejabberd
spec:
  generators:
    - git:
        repoURL: https://github.com/luminosita/k8s-cluster-talos
        revision: ejabberd
        directories:
          - path: k8s/testlab/sss*
  template:
    metadata:
      name: '{{ path.basename }}-test'
      labels:
        me.kundun: testlab-ejabberd
      finalizers:
    spec:
      project: testlab-ejabberd
      source:
        plugin:
          name: kustomize-build-with-helm
        repoURL: https://github.com/luminosita/k8s-cluster-talos
        targetRevision: ejabberd
        path: '{{ path }}'
      destination:
        name: in-cluster
        namespace: argocd
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
