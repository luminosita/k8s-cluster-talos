apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: testlab-docker
  namespace: argocd
  labels:
    me.kundun: testlab-docker
spec:
  generators:
    - git:
        repoURL: https://github.com/luminosita/k8s-cluster-talos
        revision: docker
        directories:
          - path: k8s/testlab/*
  template:
    metadata:
      name: '{{ path.basename }}-test'
      labels:
        me.kundun: testlab-docker
      finalizers:
    spec:
      project: testlab-docker
      source:
        plugin:
          name: kustomize-build-with-helm
        repoURL: https://github.com/luminosita/k8s-cluster-talos
        targetRevision: docker
        path: '{{ path }}'
      destination:
        name: in-cluster
        namespace: argocd
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
