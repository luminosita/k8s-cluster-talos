apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: testlab-vpn-tor
  namespace: argocd
  labels:
    me.kundun: testlab-vpn-tor
spec:
  generators:
    - git:
        repoURL: https://github.com/luminosita/k8s-cluster-talos
        revision: vpn-tor
        directories:
          - path: k8s/testlab/*
  template:
    metadata:
      name: '{{ path.basename }}-test'
      labels:
        me.kundun: testlab-vpn-tor
      finalizers:
    spec:
      project: testlab-vpn-tor
      source:
        plugin:
          name: kustomize-build-with-helm
        repoURL: https://github.com/luminosita/k8s-cluster-talos
        targetRevision: vpn-tor
        path: '{{ path }}'
      destination:
        name: in-cluster
        namespace: argocd
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
