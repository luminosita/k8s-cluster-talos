apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: local-route
  namespace: lldap
spec:
  parentRefs:
    - name: insecure
      namespace: gateway
  hostnames:
    - "lldap.lan"
  rules:
    - backendRefs:
        - name: lldap
          port: 80
