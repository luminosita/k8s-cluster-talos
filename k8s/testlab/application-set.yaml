apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: testlab-nexus
  namespace: argocd
  labels:
    me.kundun: testlab-nexus
spec:
  generators:
    - git:
        repoURL: https://github.com/luminosita/k8s-cluster-talos
        revision: nexus
        directories:
          - path: k8s/testlab/*
  template:
    metadata:
      name: '{{ path.basename }}-test'
      labels:
        me.kundun: testlab-nexus
      finalizers:
    spec:
      project: testlab-nexus
      source:
        plugin:
          name: kustomize-build-with-helm
        repoURL: https://github.com/luminosita/k8s-cluster-talos
        targetRevision: nexus
        path: '{{ path }}'
      destination:
        name: in-cluster
        namespace: argocd
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
