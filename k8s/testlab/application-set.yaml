apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: testlab-loki
  namespace: argocd
  labels:
    me.kundun: testlab-loki
spec:
  generators:
    - git:
        repoURL: https://github.com/luminosita/k8s-cluster-talos
        revision: loki
        directories:
          - path: k8s/testlab/*
  template:
    metadata:
      name: '{{ path.basename }}-test'
      labels:
        me.kundun: testlab-loki
      finalizers:
    spec:
      project: testlab-loki
      source:
        plugin:
          name: kustomize-build-with-helm
        repoURL: https://github.com/luminosita/k8s-cluster-talos
        targetRevision: loki
        path: '{{ path }}'
      destination:
        name: in-cluster
        namespace: argocd
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
