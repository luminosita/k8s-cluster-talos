apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: testlab-cloudflare
  namespace: argocd
  labels:
    me.kundun: testlab-cloudflare
spec:
  generators:
    - git:
        repoURL: https://github.com/luminosita/k8s-cluster-talos
        revision: cloudflare
        directories:
          - path: k8s/testlab/*
  template:
    metadata:
      name: '{{ path.basename }}-test'
      labels:
        me.kundun: testlab-cloudflare
      finalizers:
    spec:
      project: testlab-cloudflare
      source:
        plugin:
          name: kustomize-build-with-helm
        repoURL: https://github.com/luminosita/k8s-cluster-talos
        targetRevision: cloudflare
        path: '{{ path }}'
      destination:
        name: in-cluster
        namespace: argocd
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
